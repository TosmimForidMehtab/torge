## What and why

<!-- What does this change, and why is it needed? Link issues with "Fixes #123". -->

## How it was tested

<!-- Tests added or updated, and any manual verification. -->

## Checklist

- [ ] Tests cover the change, including failure paths, and pass with `-race`
- [ ] `gofmt`, `go vet` and `staticcheck` are clean
- [ ] Exported identifiers are documented; `docs/` and `README.md` updated if behavior changed
- [ ] `CHANGELOG.md` updated under `Unreleased` for user-visible changes
- [ ] No new third-party dependency in the core module's `go.mod`
